use std::ops::Deref;
use crate::utils::{Direction, Coordinates};
use crate::map::{Map, Tile};



pub struct Board{
    _map: Map,
}

impl Board{
fn move_to_marked_tile( head: &mut Tile){

}

fn move_to_free_tile( head: &mut Tile){

}

fn move_to_separator_tile( head: &mut Tile){

}

pub fn update(&mut self, move_dir : Direction){  

}


}
